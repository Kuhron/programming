// public class PathTree {
//     // tree structure that stores which paths between nodes are possible in the BoggleGrid
//     // save space vs enumerating every path as a separate array
//     private static int[][] WHOLE_GRID_ROOT = [-1, -1];  // root node of the whole tree, from which each point in the grid is a child
// 
// 
// }
// 
// public class PathTreeNode {
//     int[][] currentPosition;
//     PathTreeNode[] nextNodes;
// 
//     public PathTreeNode(int[][] currentPosition, PathTreeNode[] nextNodes) {
//         this.currentPosition = currentPosition;
//         this.nextNodes = nextNodes;
//     }
// }
